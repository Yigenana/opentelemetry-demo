services:
  otel-collector:
    command:
      - "--config=/etc/otelcol-config.yml"
      - "--config=/etc/${OTEL_SAMPLING_CONFIG}"
      - "--feature-gates=datadog.EnableOperationAndResourceNameV2"
    environment:
      - DD_SITE_PARAMETER=datadoghq.com	
      - DD_API_KEY=${DD_API_KEY}
